# 13 AI Agent Panel

## Feature

The bottom drawer AI Agent tab, settings persistence, chat history, and model selection behaviors described in `project/TEST_MATRIX.md` under scenario IDs `F13-S01` through `F13-S07`.

Implementation touchpoints include `ui/shell/BottomDrawer.tsx`, `ui/shell/AgentPanel.tsx`, and `ui/ai/**`.
