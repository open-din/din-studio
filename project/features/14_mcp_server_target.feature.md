# 14 MCP Server Target

## Feature

The DIN Studio MCP server build (`targets/mcp/**`): stdio entry, bridge server, runtime tools, and automated coverage in `targets/mcp/tests/**`. Scenario IDs `F14-S01` and `F14-S02` in `project/TEST_MATRIX.md` track this surface.
