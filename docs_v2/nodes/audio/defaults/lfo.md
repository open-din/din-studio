# AUDIO — LFO (`lfo`)

## Summary

Low-frequency modulation oscillator (`os.osc`) with depth scaling.

## Ports

- **Inputs:** `rate`, `depth`.
- **Outputs:** `out` (audio-rate modulation signal).

## Faust notes

- Uses **oscillators.lib**; range suitable for sub-audio modulation.
