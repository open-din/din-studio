# AUDIO — Reverb (`reverb`)

## Summary

Dry/wet blend with a delay tap; upgrade path to `re.zita_*` when sample rate and tail are fixed in codegen.

## Ports

- **Inputs:** `in`, `decay`, `mix`.
- **Outputs:** `out`.

## Faust notes

- See [reverbs](https://faustlibraries.grame.fr/libs/reverbs/) for production algorithms.
