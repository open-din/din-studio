# AUDIO — Delay (`delay`)

## Summary

Feedback delay line with `de.fdelay` (**delays.lib**).

## Ports

- **Inputs:** `in`, `delayTime`, `feedback`.
- **Outputs:** `out`.

## Faust notes

- Classic `+~` feedback form; max delay length fixed in snippet.
