name: gain
color: "#44cc44"
icon: ◧
label: null
description: Gain in dB (ba.db2linear) on the audio bus.
type: dsp
tags:
  - gain
  - trim
  - level
  - faust
  - stdfaust
customComponent: null
inputs:
  - type: audio
    name: in
    interface: input
  - type: float
    name: gain
    interface: slider
outputs:
  - type: audio
    name: out
    interface: input
dsp: |
  import("stdfaust.lib");
  process = _ * ba.db2linear(hslider("gain", 0, -60, 24, 0.1));
