name: auxSend
label: Aux Send
description: Tap a bus and send a controllable amount to an aux path (e.g. reverb send).
type: dsp
tags:
  - aux-send
  - routing
customComponent: null
inputs:
  - type: audio
    name: in
    interface: input
  - type: float
    name: sendGain
    interface: slider
outputs:
  - type: audio
    name: out
    interface: input
dsp: |
  process = _,_;
