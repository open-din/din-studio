name: stepSequencer
color: "#dddddd"
icon: 🎹
label: Step Sequencer
description: Step-based pattern sequencer with transport input and per-step triggers.
type: timeline
tags:
  - step-sequencer
  - sources
# Registry key → ui/editor/nodeWidgetComponents/layouts/StepSequencerNodeView.tsx
customComponent: stepSequencer
inputs:
  - type: trigger
    name: transport
    interface: input
outputs:
  - type: trigger
    name: trigger
    interface: input
