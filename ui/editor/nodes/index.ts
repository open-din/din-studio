/**
 * YAML-driven graph node views — see {@link createDynamicNode}.
 * Legacy per-type implementations live under `olds/nodes/` (reference only).
 */
export { createDynamicNode } from './DynamicNode';
export { inferHandleKind } from './inferHandleKind';
