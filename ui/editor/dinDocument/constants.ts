/** DinDocument v1 interchange identifiers (aligned with din-core `din-document`). */
export const DIN_DOCUMENT_FORMAT = 'open-din/din-document' as const;
export const DIN_DOCUMENT_VERSION = 1 as const;

/** Extension bucket for the React Flow studio graph until it is promoted to a core scene route model. */
export const STUDIO_GRAPH_EXTENSION_KEY = 'open-din/studioGraph' as const;
